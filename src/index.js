import React from "react";
import App from "./App";
import ReactDOM from "react-dom";

const StudentDatabse = () => {
    return (
            <App />
    );
};

ReactDOM.render(
    <StudentDatabse />,
    document.querySelector("#root")
);